<div class="container">
    {{#unless posts }}
    <p class="center-align">No posts to display</p>
    {{/unless}}
    <div class="row">
        {{#each posts}}
        {{> post-info loggedIn=../loggedIn}}
        {{/each}}
    </div>
</div>
<script src="/js/homepage.js"></script>